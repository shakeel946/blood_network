const app=document.getElementById('app'),nav=document.getElementById('nav');let token=localStorage.getItem('bn_token'),role=localStorage.getItem('bn_role');
const api=async(path,opts={})=>{opts.headers={...(opts.headers||{}),...(token?{Authorization:`Bearer ${token}`}:{})};if(opts.body&&!(opts.body instanceof FormData))opts.headers['Content-Type']='application/json';const r=await fetch(path,opts);let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.detail||'Request failed');return d};
function logout(){localStorage.clear();token=null;role=null;renderLogin()};function renderLogin(){nav.innerHTML='';app.innerHTML=`<section class="container auth"><div class="card hero"><div>🩸</div><h1>Blood Network</h1><p>Connect willing blood donors with a secure donor registry.</p></div><div class="card"><h2>Donor Login</h2><p class="muted">Access your donor profile.</p><form id="login" class="grid"><div class="field full"><label>Email</label><input name="email" type="email" required></div><div class="field full"><label>Password</label><input name="password" type="password" minlength="8" required></div><div class="full actions"><button>Login</button><button type="button" class="secondary" id="signupBtn">Create account</button></div></form><div id="msg" class="msg"></div></div></section>`;document.getElementById('login').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target);try{let d=await api('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});localStorage.setItem('bn_token',d.access_token);localStorage.setItem('bn_role',d.role);token=d.access_token;role=d.role;render()}catch(x){document.getElementById('msg').textContent=x.message}};document.getElementById('signupBtn').onclick=renderSignup}
function renderSignup(){app.innerHTML=`<section class="container"><div class="card"><h2>Become a Blood Donor</h2><p class="muted">Create your account and donor profile.</p><form id="signup" class="grid"><div class="field"><label>Full Name</label><input name="full_name" required></div><div class="field"><label>Age</label><input name="age" type="number" min="18" max="65" required></div><div class="field"><label>Gender</label><select name="gender"><option>Male</option><option>Female</option><option>Other</option><option>Prefer not to say</option></select></div><div class="field"><label>Blood Group</label><select name="blood_group"><option>A+</option><option>A-</option><option>B+</option><option>B-</option><option>AB+</option><option>AB-</option><option>O+</option><option>O-</option></select></div><div class="field"><label>Phone Number</label><input name="phone" required></div><div class="field"><label>Email</label><input name="email" type="email" required></div><div class="field full"><label>Address</label><textarea name="address" required></textarea></div><div class="field"><label>Last Blood Donation Date</label><input name="last_donation_date" type="date"></div><div class="field"><label>Available to Donate</label><select name="available_to_donate"><option value="true">Yes</option><option value="false">No</option></select></div><div class="field"><label>Password</label><input name="password" type="password" minlength="8" required></div><div class="full actions"><button>Create account</button><button type="button" class="secondary" id="back">Back</button></div></form><div id="msg" class="msg"></div></div></section>`;document.getElementById('back').onclick=renderLogin;document.getElementById('signup').onsubmit=async e=>{e.preventDefault();let o=Object.fromEntries(new FormData(e.target));o.age=Number(o.age);o.available_to_donate=o.available_to_donate==='true';if(!o.last_donation_date)delete o.last_donation_date;try{await api('/api/auth/register',{method:'POST',body:JSON.stringify(o)});document.getElementById('msg').textContent='Account created. You can now log in.';setTimeout(renderLogin,700)}catch(x){document.getElementById('msg').textContent=x.message}}}
async function renderDonor(){let d=await api('/api/me');nav.innerHTML=`<button class="secondary" onclick="renderDonor()">My Profile</button><button onclick="logout()">Logout</button>`;app.innerHTML=`<section class="container"><div class="card"><div class="profile-head">${d.profile_photo?`<img class="photo" src="${d.profile_photo}">`:'<div class="photo" style="display:grid;place-items:center;background:#fee2e2">🩸</div>'}<div><h1>${esc(d.full_name)}</h1><span class="pill ${d.available_to_donate?'available':'unavailable'}">${d.available_to_donate?'Available to donate':'Currently unavailable'}</span></div></div><hr style="border:0;border-top:1px solid #eee;margin:22px 0"><form id="profile" class="grid">${field('Full Name','full_name',d.full_name)}${field('Age','age',d.age,'number')}<div class="field"><label>Gender</label><select name="gender">${['Male','Female','Other','Prefer not to say'].map(x=>`<option ${x===d.gender?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>Blood Group</label><select name="blood_group">${['A+','A-','B+','B-','AB+','AB-','O+','O-'].map(x=>`<option ${x===d.blood_group?'selected':''}>${x}</option>`).join('')}</select></div>${field('Phone Number','phone',d.phone)}${field('Email','email',d.email,'email')}${field('Address','address',d.address,'text',true)}${field('Last Donation Date','last_donation_date',d.last_donation_date||'','date')}<div class="field"><label>Available to Donate</label><select name="available_to_donate"><option value="true" ${d.available_to_donate?'selected':''}>Yes</option><option value="false" ${!d.available_to_donate?'selected':''}>No</option></select></div><div class="field full"><label>Profile Photo</label><input id="photo" type="file" accept="image/png,image/jpeg,image/webp"></div><div class="full actions"><button>Save changes</button></div></form><div id="msg" class="msg"></div></div><div class="card" style="margin-top:20px"><h2>Donation History</h2>${d.donations.length?`<table><tr><th>Date</th><th>Details</th></tr>${d.donations.map(x=>`<tr><td>${x.donation_date}</td><td>${esc(x.details||'—')}</td></tr>`).join('')}</table>`:'<p class="muted">No donation history has been recorded yet.</p>'}</div></section>`;document.getElementById('profile').onsubmit=async e=>{e.preventDefault();let o=Object.fromEntries(new FormData(e.target));o.age=Number(o.age);o.available_to_donate=o.available_to_donate==='true';if(!o.last_donation_date)delete o.last_donation_date;try{await api('/api/me',{method:'PATCH',body:JSON.stringify(o)});let f=document.getElementById('photo').files[0];if(f){let fd=new FormData();fd.append('file',f);await api('/api/me/photo',{method:'POST',body:fd})}document.getElementById('msg').textContent='Profile updated.';renderDonor()}catch(x){document.getElementById('msg').textContent=x.message}}}
function field(label,name,value,type='text',full=false){return `<div class="field ${full?'full':''}"><label>${label}</label><input name="${name}" type="${type}" value="${esc(value)}" required></div>`}
async function renderAdmin(){let s=await api('/api/admin/stats');nav.innerHTML=`<button class="secondary" onclick="renderAdmin()">Dashboard</button><button onclick="logout()">Logout</button>`;app.innerHTML=`<section class="container"><h1>Admin Dashboard</h1><div class="grid three"><div class="card"><div class="muted">Total donors</div><div class="stat">${s.total_donors}</div></div><div class="card"><div class="muted">Available</div><div class="stat">${s.available_donors}</div></div><div class="card"><div class="muted">Disabled</div><div class="stat">${s.disabled_donors}</div></div></div><div class="card" style="margin-top:20px"><div class="toolbar"><input id="search" placeholder="Search name, email, phone"><select id="bg"><option value="">All blood groups</option>${['A+','A-','B+','B-','AB+','AB-','O+','O-'].map(x=>`<option>${x}</option>`).join('')}</select><select id="av"><option value="">All availability</option><option value="true">Available</option><option value="false">Unavailable</option></select><button onclick="loadDonors()">Search</button></div><div class="table-wrap"><table id="donors"></table></div></div><div id="detail" style="margin-top:20px"></div></section>`;loadDonors()}
async function loadDonors(){let q=new URLSearchParams();let s=document.getElementById('search').value,b=document.getElementById('bg').value,a=document.getElementById('av').value;if(s)q.set('search',s);if(b)q.set('blood_group',b);if(a)q.set('available',a);let ds=await api('/api/donors?'+q);document.getElementById('donors').innerHTML=`<tr><th>Name</th><th>Blood</th><th>Age</th><th>Phone</th><th>Availability</th><th>Action</th></tr>`+ds.map(d=>`<tr><td>${esc(d.full_name)}</td><td><b>${d.blood_group}</b></td><td>${d.age}</td><td>${esc(d.phone)}</td><td><span class="pill ${d.available_to_donate?'available':'unavailable'}">${d.available_to_donate?'Available':'Unavailable'}</span></td><td><button onclick="showDonor(${d.id})">View</button></td></tr>`).join('')}
async function showDonor(id){let d=await api('/api/donors/'+id);document.getElementById('detail').innerHTML=`<div class="card"><div class="profile-head">${d.profile_photo?`<img class="photo" src="${d.profile_photo}">`:'<div class="photo" style="display:grid;place-items:center;background:#fee2e2">🩸</div>'}<div><h2>${esc(d.full_name)}</h2><p class="muted">${esc(d.email)} · ${esc(d.phone)}</p></div></div><div class="grid" style="margin-top:20px">${field('Full Name','full_name',d.full_name)}${field('Age','age',d.age,'number')}<div class="field"><label>Gender</label><select id="e_gender"><option>Male</option><option>Female</option><option>Other</option><option>Prefer not to say</option></select></div><div class="field"><label>Blood Group</label><select id="e_bg">${['A+','A-','B+','B-','AB+','AB-','O+','O-'].map(x=>`<option ${x===d.blood_group?'selected':''}>${x}</option>`).join('')}</select></div>${field('Phone','phone',d.phone)}${field('Email','email',d.email,'email')}${field('Address','address',d.address,'text',true)}${field('Last Donation','last_donation_date',d.last_donation_date||'','date')}<div class="field"><label>Availability</label><select id="e_av"><option value="true" ${d.available_to_donate?'selected':''}>Available</option><option value="false" ${!d.available_to_donate?'selected':''}>Unavailable</option></select></div><div class="field"><label>Account Status</label><select id="e_status"><option ${d.account_status==='ACTIVE'?'selected':''}>ACTIVE</option><option ${d.account_status==='DISABLED'?'selected':''}>DISABLED</option></select></div></div><div class="actions"><button onclick="saveAdmin(${d.id})">Save</button><button class="secondary" onclick="addDonation(${d.id})">Record donation</button></div><h3>Donation History</h3>${d.donations.length?d.donations.map(x=>`<p>${x.donation_date} — ${esc(x.details||'No details')}</p>`).join(''):'<p class="muted">No donations recorded.</p>'}</div>`;document.querySelectorAll('#detail input').forEach(i=>i.dataset.key=i.name);document.getElementById('e_gender').value=d.gender}
async function saveAdmin(id){let c=document.querySelectorAll('#detail input'),o={};c.forEach(x=>o[x.name]=x.value);o.age=Number(o.age);o.gender=document.getElementById('e_gender').value;o.blood_group=document.getElementById('e_bg').value;o.available_to_donate=document.getElementById('e_av').value==='true';o.account_status=document.getElementById('e_status').value;if(!o.last_donation_date)delete o.last_donation_date;try{await api('/api/donors/'+id,{method:'PATCH',body:JSON.stringify(o)});showDonor(id);loadDonors()}catch(e){alert(e.message)}}
async function addDonation(id){let date=prompt('Donation date (YYYY-MM-DD):');if(!date)return;let details=prompt('Details (optional):')||null;try{await api('/api/donors/'+id+'/donations',{method:'POST',body:JSON.stringify({donation_date:date,details})});showDonor(id);loadDonors()}catch(e){alert(e.message)}}
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function render(){if(!token)return renderLogin();if(role==='ADMIN')return renderAdmin();return renderDonor()}render();
